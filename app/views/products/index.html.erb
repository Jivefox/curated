<% if flash[:alert] %>
    <div class="alert"><%= flash[:alert] %></div>
<% end %>

<h1>Showing all products</h1>

<% @products.each do |product| %>
    <li>
        <%= link_to product.name, product_path(product) %>
    </li>
<% end %><br>

<%= link_to "Sell something", new_product_path %>